import { OutfitListFilterPipe } from './outfit-list-filter.pipe';

describe('OutfitListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OutfitListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
