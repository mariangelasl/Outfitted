import { TemporadaFilterPipe } from './temporada-filter.pipe';

describe('TemporadaFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TemporadaFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
