import { EstiloFilterPipe } from './estilo-filter.pipe';

describe('EstiloFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EstiloFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
