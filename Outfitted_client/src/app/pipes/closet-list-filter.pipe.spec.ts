import { ClosetListFilterPipe } from './closet-list-filter.pipe';

describe('ClosetListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ClosetListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
